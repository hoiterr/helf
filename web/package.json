{
  "name": "helf-web",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "setup": "npm --prefix ../design-system install && npm --prefix ../design-system run build",
    "predev": "npm run setup",
    "prebuild": "npm run setup",
    "dev": "next dev -p 3001",
    "build": "next build",
    "start": "next start -p 3001",
    "lint": "next lint"
  },
  "dependencies": {
    "@helf/ui": "file:../design-system",
    "next": "^14.2.5",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/node": "^20.12.12",
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "typescript": "^5.4.5"
  }
}
