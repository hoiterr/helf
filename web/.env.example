# Base URL of the helf backend API (the NestJS app, default port 3000).
NEXT_PUBLIC_API_URL=http://localhost:3000
# Demo user the seeded backend creates. Replace once real auth lands.
NEXT_PUBLIC_DEMO_USER_ID=demo-user
